import { View, Text } from 'react-native'
import * as React from 'react';

const GerarRelatorios = ({ navigation }) => {
  return (
    <View>
      <Text>Gerar Relatórios</Text>
    </View>
  );
};
export default GerarRelatorios
