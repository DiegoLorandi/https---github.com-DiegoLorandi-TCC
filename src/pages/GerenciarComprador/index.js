import { View, Text } from 'react-native'
import * as React from 'react';
const GerenciarComprador = ({ navigation }) => {
  return (
    <View>
      <Text>Gerenciar Comprador</Text>
    </View>
  );
};
export default GerenciarComprador
